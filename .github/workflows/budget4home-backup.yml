name: budget4home-backup

on:
  schedule:
    # run every day at 12
    - cron: '0 12 * * *'

jobs:
  backup:
    name: backup 💾
    runs-on: ubuntu-latest
    steps:
      - name: backup 💾
        run: |
          curl -X POST -H "backup: 787421ff-1e47-4e61-8289-12f25acc3a55" https://budget4home.vercel.app/api/backup
        shell: bash
