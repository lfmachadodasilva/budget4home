import styles from './web-components.module.scss';

export function WebComponents() {
  return (
    <div className={styles['container']}>
      <h1>Welcome to WebComponents!</h1>
    </div>
  );
}

export default WebComponents;
