# firesore

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build firesore` to build the library.

## Running unit tests

Run `nx test firesore` to execute the unit tests via [Jest](https://jestjs.io).
