# common

Shared utilities and types used across Budget4Home applications.

## Features

- Common utility functions (formatCurrency, formatDate, etc.)
- Shared TypeScript types and interfaces
- Reusable business logic

## Usage

```typescript
import { formatCurrency, Budget, Transaction } from 'common'

const amount = formatCurrency(1234.56)
```
